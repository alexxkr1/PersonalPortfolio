@model Portfolio.Core.Domain.Post

<div>
    <form asp-controller="Home" asp-action="Edit" method="post">
        <input asp-for="Id" type="hidden" />
        <div>
            <label>Title</label>
            <input asp-for="Title" />
        </div>
        <div>
            <label>Body</label>
            <input asp-for="body" />
        </div>
        <input type="submit" value="Submit"/>
    </form>
</div>
